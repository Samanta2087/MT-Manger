<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MTManagerLite">

        <!-- ── Main launcher ── -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Locate its location (Open file manager at the file's parent path) -->
            <intent-filter android:label="XYvion Locate file" android:icon="@mipmap/ic_launcher">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Match all files with the file:// scheme -->
                <data android:scheme="file" android:mimeType="*/*" />
                <!-- Match all files with the content:// scheme -->
                <data android:scheme="content" android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- ── File Editor — exported so other apps can open files with us ── -->
        <activity
            android:name=".ui.editor.FileEditorActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:label="XYvion – Editor">

            <!-- Open any plain text file -->
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <!-- Specific code / script mimetypes -->
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
            </intent-filter>
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/xml" />
            </intent-filter>
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/javascript" />
            </intent-filter>
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>

            <!-- Open files by URI (file:// scheme) — any extension we handle -->
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" android:mimeType="*/*" />
            </intent-filter>

            <!-- Content URI scheme (needed for most modern file managers) -->
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="text/*" />
            </intent-filter>
            <intent-filter android:label="Edit with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <!-- ── Image Viewer ── -->
        <activity
            android:name=".ui.viewer.ImageViewerActivity"
            android:exported="true"
            android:label="XYvion – Viewer">

            <intent-filter android:label="View with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- ── Video Player ── -->
        <activity
            android:name=".ui.viewer.VideoPlayerActivity"
            android:exported="true"
            android:label="XYvion – Video"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="fullSensor">

            <intent-filter android:label="Play with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter android:label="Play with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- ── Audio Player ── -->
        <activity
            android:name=".ui.viewer.AudioPlayerActivity"
            android:exported="true"
            android:label="XYvion – Audio">

            <intent-filter android:label="Play with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter android:label="Play with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- ── PDF Viewer ── -->
        <activity
            android:name=".ui.viewer.PdfViewerActivity"
            android:exported="true"
            android:label="XYvion – PDF Viewer">

            <!-- application/pdf — used by WhatsApp, Gmail, Drive, etc. -->
            <intent-filter android:label="Open with XYvion PDF">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" />
            </intent-filter>
            <!-- content:// scheme (WhatsApp shares as content://) -->
            <intent-filter android:label="Open with XYvion PDF">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="application/pdf" />
            </intent-filter>
            <!-- file:// scheme -->
            <intent-filter android:label="Open with XYvion PDF">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" android:mimeType="application/pdf" />
            </intent-filter>
        </activity>

        <!-- ── CSV / TSV Viewer ── -->
        <activity
            android:name=".ui.viewer.CsvViewerActivity"
            android:exported="true"
            android:label="XYvion – Spreadsheet Viewer">

            <intent-filter android:label="Open with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/csv" />
            </intent-filter>
            <intent-filter android:label="Open with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/tab-separated-values" />
            </intent-filter>
            <intent-filter android:label="Open with XYvion">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="text/csv" />
            </intent-filter>
        </activity>

        <!-- ── DOCX Viewer ── -->
        <activity
            android:name=".ui.viewer.DocxViewerActivity"
            android:exported="false"
            android:label="XYvion – Document Viewer" />

        <!-- ── Search ── -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- ── FileProvider for sharing ── -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
